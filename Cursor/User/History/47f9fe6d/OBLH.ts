// Notifications temporarily disabled for Expo Go with SDK 53.
// Use a dev build to re-enable and replace these stubs with expo-notifications logic.

export async function requestNotificationPermissions() {
  return false;
}

export async function scheduleDailyReminder(_hour: number, _minute: number, _body: string) {
  return null as any;
}



